After an update zen-browser often picks the wrong profile for startup, here is how to fix it:  
Go to ~/.zen/profiles.ini  
set start with last profile to 0  
start zen  
pick the Default profile (not Default Release)  
close zen  
set start with last profile back to 1  